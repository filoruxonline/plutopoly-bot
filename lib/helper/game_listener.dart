class GameListener {
  /// gets called when the bot initializes
  onInit() {}

  /// gets called when all players have played 1 round.
  onNewTurn() {}

  /// When the next player has moved
  onNextPlayer() {}

  /// low-level, gets called when new data is received.
  onUpdate() {}
}
