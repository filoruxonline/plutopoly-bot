import 'package:PlutopolyExtension/helper/game.dart';

//Enter game pin here (replace null)
const String GAMEPIN = null;
const bool VERBOSE = true;

//Go to lib/main.dart to start building your Bot.
void main() async {
  Helper.initBot(GAMEPIN, VERBOSE);
}
